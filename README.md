# NandaGuessr

Угадывай персонажей по признакам и картинкам — без лимитов на день: Наруто, Dota 2, Атака титанов и Блич. Неофициальный фанатский проект.

- **По признакам** — вводишь любого персонажа, клетки показывают совпадение: зелёная — совпадает, жёлтая — частично, серая — мимо; ↑/↓ — ответ позже/раньше (или больше/меньше).
- **По картинке** — угадай по сильно увеличенному фрагменту; каждая ошибка немного отдаляет картинку.

Название — в `src/brand.ts`. После входа открывается главная (дашборд): разделы «Аниме» и «Игры», у каждой вселенной свои режимы. Профиль (клик по нику вверху) — ник, статистика по всем играм и режимам, сброс статистики.

Языки интерфейса: RU / UK / EN (`src/i18n`). Тексты интерфейса — `src/i18n/ui.ts`, переводы значений из данных — `src/i18n/values.ts`, имена: EN — оригинальные, UK — автоматически из русских.

Новая вселенная — файл в `src/games/` + запись в `src/games/index.ts`; новый режим — запись в `src/modes.ts` и id в `modes` у вселенной (пока не готов — `UPCOMING_MODES`, показывается как «скоро»).

## Запуск

```bash
npm install
npm run dev
```

## Аккаунты

Играть можно только после входа. Serverless-функции Vercel в `api/`: `auth/*` (`register`, `login`, `logout`, `me`), `profile/nickname`, `stats/record`, `stats/reset`. Пользователи, ники и статистика хранятся в MongoDB (коллекция `users`, пароли — scrypt), сессия — JWT в HttpOnly-cookie на 30 дней. Ошибки API — коды (`taken`, `bad_credentials`…), текст подставляет клиент на нужном языке.

Нужны переменные окружения (в Vercel → Settings → Environment Variables, локально — в `.env.local`, пример в `.env.example`):

- `MONGODB_URI` — строка подключения MongoDB Atlas;
- `AUTH_SECRET` — случайная строка минимум из 32 символов;
- `MONGODB_DB` — имя базы (необязательно, по умолчанию `nandaguessr`).

`npm run dev` поднимает эти функции и локально (плагин в `vite.config.ts`).

## Данные

`npm run data` пересобирает все игры (`npm run data:naruto`, `data:dota`, `data:aot`, `data:bleach` — по отдельности).

**Наруто** (`scripts/build-data.mjs`, переводы в `scripts/ru.mjs`)
- персонажи — [Dattebayo API](https://dattebayo-api.onrender.com), только канон манги до главы 699;
- картинки и русские имена — Naruto Wiki (Fandom); имён без русской статьи транслитерируются;
- загадываются топ-150 по длине статьи на вики.

**Dota 2** (`scripts/build-dota.mjs`, ручные данные в `scripts/dota-heroes.mjs`)
- атрибут и сложность — datafeed Valve, роли и тип атаки — OpenDota, год выхода и лор-арт — Dota 2 Wiki (Fandom);
- пол, раса и русские прозвища для поиска — вручную.

**Атака титанов** (`scripts/build-aot.mjs`)
- всё из Attack on Titan Wiki (Fandom): вид, принадлежность, род занятий, сила титана, статус, арка дебюта; цветные картинки из аниме-страниц персонажей;
- загадываются ~70 известных персонажей (список `ANSWER_POOL` в скрипте).

**Блич** (`scripts/build-bleach.mjs`)
- всё из Bleach Wiki (Fandom): раса, принадлежность, должность, отряд, силы (шикай/банкай/…), арка дебюта;
- загадываются топ-130 по «избранному» на MyAnimeList (через Jikan, кэш в `.cache/mal-269.json`).

Общие хелперы для вики и картинок — `scripts/lib.mjs`.

Миниатюры собираются в один атлас (`thumbs.webp`), чтобы грузились одним запросом. Кэш запросов — в `.cache/` (не в git).

## Деплой

Vercel, подключён к GitHub-репозиторию: каждый push в `main` деплоится автоматически (Framework Preset: Vite, Build Command: `npm run build`, Output: `dist`).
